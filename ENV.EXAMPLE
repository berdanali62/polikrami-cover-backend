NODE_ENV=development
PORT=3000

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/coverpolikrami?schema=public
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=coverpolikrami

JWT_ACCESS_SECRET=your_super_secure_access_secret_at_least_64_characters_long_for_production
JWT_REFRESH_SECRET=your_super_secure_refresh_secret_at_least_64_characters_long_for_production
ACCESS_EXPIRES_IN=900s
REFRESH_EXPIRES_IN=30d

COOKIE_DOMAIN=
COOKIE_SECURE=false
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080

APP_URL=http://localhost:3000
CDN_URL=

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@example.com
CONTACT_TO=
EMAIL_REDIRECT_TO=
SMTP_TLS_INSECURE=false

UPLOAD_DIR=./uploads
UPLOAD_MAX_SIZE_MB=100
UPLOAD_ALLOWED_MIME=image/png,image/jpeg,image/webp,application/pdf

# Business Configuration
SHIPPING_COST_CENTS=3000
PASSWORD_RESET_CODE_EXPIRE_MINUTES=10
EMAIL_VERIFY_CODE_EXPIRE_MINUTES=60

# Email Queue Cleanup
EMAIL_QUEUE_CLEANUP_DAYS=30
EMAIL_QUEUE_FAILED_CLEANUP_HOURS=24

# Payment Provider Configuration
PAYMENT_PROVIDER=mock
IYZICO_API_KEY=your_iyzico_api_key
IYZICO_SECRET_KEY=your_iyzico_secret_key
IYZICO_BASE_URL=https://sandbox-api.iyzipay.com

